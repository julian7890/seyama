generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

model schedule {
  id   String   @id @default(auto()) @map("_id") @db.ObjectId
  date DateTime
  en   Json?
  jp   Json?
  link String?
}
